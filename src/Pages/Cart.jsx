import React from "react";
import CartDisplay from "../Components/CartDisplay/CartDisplay";

const Cart = () => {
    return (
        <div>
        <CartDisplay />
        </div>
    );
    };

export default Cart;